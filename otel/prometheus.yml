global:
  scrape_interval: 15s

scrape_configs:
  # Unico target: el endpoint que expone el exporter prometheus del Collector.
  # La app no se scrapea directo, exporta por OTLP al Collector.
  - job_name: otel-collector
    static_configs:
      - targets: ['otel-collector:8889']
